package clay.utils;

enum abstract BlendMode(Int) {
    var UNDEFINED;
    var NONE;
    var NORMAL;
    var ADD;
    var MULTIPLY;
    var SCREEN;
    var ERASE;
    var MASK;
    var BELOW;
    var CUSTOM;
}